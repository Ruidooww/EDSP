# IP-Guard UEBA AI 自学习架构设计

> 从规则引擎到 AI 驱动：无监督异常检测 + 有监督分类 + 自学习反馈闭环 + 行业模型

---

## 一、AI 学习架构总览

```
                        ┌─────────────────────────────────────┐
                        │         IP-Guard 原始日志             │
                        │  DOC_LOG | UDISK_LOG | MAIL_LOG ...  │
                        └──────────────┬──────────────────────┘
                                       │
                        ┌──────────────▼──────────────────────┐
                        │        特征工程 (Feature Store)       │
                        │  ┌────────────────────────────────┐  │
                        │  │ 用户级: 行为基线/风险因子/时段   │  │
                        │  │ 文件级: 类型/大小/路径/敏感度    │  │
                        │  │ 时间级: 频率/时段/间隔/突发     │  │
                        │  │ 关系级: 用户-文件/用户-设备/流转  │  │
                        │  └────────────────────────────────┘  │
                        └──────────────┬──────────────────────┘
                                       │
         ┌─────────────────────────────┼──────────────────────────┐
         │                             │                          │
         ▼                             ▼                          ▼
┌──────────────────┐    ┌──────────────────────┐    ┌──────────────────────┐
│  无监督学习       │    │   有监督学习           │    │   强化学习 (RL)       │
│  (冷启动)        │    │   (优化阶段)          │    │   (策略优化)          │
│                  │    │                      │    │                      │
│ • IsolationForest│    │ • XGBoost/LightGBM   │    │ • 动态阈值调整       │
│ • AutoEncoder    │    │ • 异常分类(TP vs FP) │    │ • 告警升级策略       │
│ • DBSCAN 聚类    │    │ • 风险等级预测       │    │ • 自适应响应         │
│ • OneClassSVM    │    │ • 用户画像分类       │    │ • AutoML 选型        │
└────────┬─────────┘    └──────────┬───────────┘    └──────────┬───────────┘
         │                         │                          │
         └─────────────────────────┼──────────────────────────┘
                                   │
                    ┌──────────────▼──────────────────────┐
                    │         告警聚合 & 评分              │
                    │  多模型加权投票 → 最终风险分          │
                    └──────────────┬──────────────────────┘
                                   │
         ┌─────────────────────────┼──────────────────────────┐
         │                         │                          │
         ▼                         ▼                          ▼
┌──────────────────┐    ┌──────────────────────┐    ┌──────────────────────┐
│  自动处置         │    │   人工审核             │    │   标注反馈            │
│  (高风险自动阻断) │    │   (中等风险人工判断)   │    │   (打标签入库)         │
└──────────────────┘    └──────────┬───────────┘    └──────────┬───────────┘
                                   │                          │
                                   └──────────┬───────────────┘
                                              │
                                   ┌──────────▼───────────┐
                                   │     标注数据库         │
                                   │  (Labeled Dataset)    │
                                   └──────────┬───────────┘
                                              │
                                   ┌──────────▼───────────┐
                                   │   定期模型重训练       │
                                   │  (Weekly/Bi-weekly)  │
                                   └──────────────────────┘
```

---

## 二、模型矩阵

### 2.1 模型与适用场景

| 模型 | 类型 | 场景 | 输入 | 输出 |
|------|------|------|------|------|
| **Isolation Forest** | 无监督 | 冷启动异常检测 | 用户行为特征向量 | 异常分数 [0-1] |
| **AutoEncoder** | 无监督 | 重构误差检测 | 时序行为序列 | 重建误差 → 异常分 |
| **DBSCAN** | 无监督 | 用户群体聚类 | 行为特征向量 | 聚类标签 + 离群点 |
| **XGBoost** | 有监督 | 告警真伪分类 | 告警特征 + 上下文 | TP/FP 概率 |
| **LightGBM** | 有监督 | 风险等级预测 | 用户画像特征 | 风险等级 1-5 |
| **LSTM** | 有监督 | 时序行为预测 | 30天行为序列 | 下一天行为预测 |
| **Transformer** | 有监督 | 跨渠道关联 | 多渠道时间序列 | 关联异常分 |
| **BERT (微调)** | 有监督 | 敏感内容识别 | 文本(邮件/IM/文件名) | 敏感度分类 |

### 2.2 行业特化模型

```
基础模型 (Base Model)
  训练数据: 通用DLP事件 + 公开威胁情报
  │
  ├── 金融行业微调 (Fine-tune)
  │   特征: 交易数据模式 / 客户信息保护 / 监管关键词 / 合规时间窗口
  │   
  ├── 制造业微调 (Fine-tune)
  │   特征: 图纸文件模式 / 供应链通信 / 工艺文档 / CAD操作
  │   
  ├── 科技/互联网微调 (Fine-tune)
  │   特征: 源码保护 / API密钥 / 云服务 / 代码仓库模式
  │   
  └── 政府/军工微调 (Fine-tune)
      特征: 密级文件 / 等保合规 / 物理隔离 / 审批链
```

---

## 三、自学习闭环 (Continuous Learning Loop)

### 3.1 学习循环

```
  ┌─────────────────────────────────────────────────────┐
  │                                                     │
  │  ① 数据采集 ──→ ② 特征工程 ──→ ③ 模型推理          │
  │       ↑                              │              │
  │       │                              ▼              │
  │       │                     ④ 告警输出 + 置信度     │
  │       │                              │              │
  │       │                   ┌──────────┴──────────┐   │
  │       │                   │                     │   │
  │       │             高置信度(>85%)        低置信度    │
  │       │                   │                     │   │
  │       │             自动处置            ⑤ 人工审核   │
  │       │                   │                     │   │
  │       │                   └──────────┬──────────┘   │
  │       │                              │              │
  │       │                     ⑥ 标注结果入库          │
  │       │                              │              │
  │       └──────── ⑦ 定期重训练 ◄──────┘              │
  │                                                     │
  └─────────────────────────────────────────────────────┘
```

### 3.2 主动学习 (Active Learning)

```
当模型对某个告警的置信度在 40%-60% 之间时:
  1. 触发"不确定样本"标记
  2. 推送到人工审核队列（优先级最高）
  3. 人工判断后立即标注
  4. 该样本立即加入训练集
  5. 下次模型更新时，对该类场景的置信度提升
```

### 3.3 半监督学习 (Semi-Supervised)

```
阶段1: 少量标注数据 + 大量未标注数据
  → 用标注数据训练初始模型
  → 用模型对未标注数据打伪标签（pseudo-labeling）
  → 选择高置信度的伪标签加入训练集
  → 迭代提升

阶段2: 自训练 (Self-Training)
  → 模型用自身预测结果扩展训练集
  → 每轮选择置信度 > 90% 的预测
  → 逐步扩大标注数据集
```

---

## 四、特征工程

### 4.1 从 IP-Guard 日志提取的特征

```python
用户行为特征向量 (每用户每天):
{
    # ── 文档操作特征 (20维) ──
    "doc_total_ops": 总操作数,
    "doc_create": 创建数, "doc_modify": 修改数, "doc_delete": 删除数,
    "doc_copy": 复制数, "doc_move": 移动数, "doc_rename": 重命名数,
    "doc_read": 读取数, "doc_read_total_size": 总读取量,
    "doc_src_device_local": 本地操作占比, "doc_src_device_network": 网络操作占比,
    "doc_dest_external": 目标为外部设备占比,
    "doc_file_types_entropy": 文件类型熵(多样性),
    "doc_size_avg": 平均文件大小, "doc_size_max": 最大文件大小,
    "doc_size_total": 总文件大小, "doc_size_std": 文件大小标准差,
    "doc_app_count": 使用的应用数, "doc_app_entropy": 应用多样性,

    # ── USB特征 (10维) ──
    "usb_total_ops": USB操作总数,
    "usb_read_count": 从U盘读, "usb_write_count": 写入U盘,
    "usb_total_size": 总传输量, "usb_unique_devices": 唯一U盘数,
    "usb_device_encrypted_ratio": 加密U盘占比,
    "usb_cross_machine": 跨机使用次数,
    "usb_after_hours_ratio": 非工作时间占比,

    # ── 邮件特征 (10维) ──
    "mail_total_sent": 发件数,
    "mail_external_ratio": 外部收件占比,
    "mail_attach_total_size": 附件总量,
    "mail_attach_count": 附件数,
    "mail_unique_recipients": 唯一收件人数,
    "mail_bcc_usage": 密送使用次数,
    "mail_subject_entropy": 主题多样性,

    # ── 时间特征 (10维) ──
    "time_work_hours_ratio": 工作时间占比,
    "time_after_hours_ratio": 非工作时间占比,
    "time_weekend_ratio": 周末占比,
    "time_hour_entropy": 时段分布熵,
    "time_burst_count": 突发操作次数(5分钟内>50次),
    "time_session_count": 会话数,
    "time_avg_session_duration": 平均会话时长,

    # ── 关系特征 (8维) ──
    "rel_unique_files": 操作的不同文件数,
    "rel_unique_dirs": 操作的不同目录数,
    "rel_sensitive_files_ratio": 敏感文件占比,
    "rel_new_files_ratio": 新文件占比(首次出现),
    "rel_peer_similarity": 与同组用户相似度,
    "rel_flow_outbound": 文件外向流转次数,

    # ── 加密特征 (5维) ──
    "encrypt_decrypt_count": 解密次数,
    "encrypt_level_change": 安全级别变更次数,
    "encrypt_unauthorized_attempts": 非授权尝试次数,
}
```

### 4.2 特征存储 (Feature Store)

```
    离线特征 (T+1批量计算)          在线特征 (实时计算)
    ┌─────────────────────┐       ┌──────────────────┐
    │ 30天行为基线         │       │ 今天实时计数器     │
    │ 用户聚类标签         │       │ 当前会话状态       │
    │ 历史告警统计         │       │ 最近1小时行为      │
    │ 同组用户基准         │       │ 突发检测           │
    │ 行业模板特征         │       │ 审批状态           │
    └─────────────────────┘       └──────────────────┘
              │                           │
              └───────────┬───────────────┘
                          ▼
              ┌─────────────────────┐
              │   模型推理输入向量    │
              │   (63维 + 实时上下文) │
              └─────────────────────┘
```

---

## 五、AI API 接口设计

### 5.1 核心接口

| 端点 | 方法 | 说明 |
|------|------|------|
| `/api/ai/score` | POST | 实时风险评分 (接受用户/事件特征, 返回风险分+置信度) |
| `/api/ai/anomalies` | GET | AI 检测的异常列表 (含模型来源和置信度) |
| `/api/ai/feedback` | POST | 提交人工标注反馈 (告警ID + 真/假 + 标签) |
| `/api/ai/models` | GET | 当前模型列表与状态 |
| `/api/ai/models/{id}/train` | POST | 触发模型重训练 |
| `/api/ai/models/{id}/metrics` | GET | 模型性能指标 (准确率/召回率/F1/AUC) |
| `/api/ai/predict` | POST | 批量预测 (用于离线分析) |
| `/api/ai/features/{user_id}` | GET | 用户特征向量 |
| `/api/ai/industry/{name}/template` | GET | 行业模型模板配置 |
| `/api/ai/label/queue` | GET | 待人工标注队列 (低置信度告警) |
| `/api/ai/label/submit` | POST | 提交标注结果 |
| `/api/ai/explain/{alert_id}` | GET | 告警可解释性 (SHAP/LIME) |

### 5.2 请求/响应示例

```json
// POST /api/ai/score
// 请求: 实时评分
{
  "user_id": 1001,
  "event_type": "doc_copy",
  "context": {
    "source_path": "D:\\Projects\\Core\\secret.docx",
    "dest_path": "F:\\",
    "file_size": 52428800,
    "app_name": "explorer.exe",
    "timestamp": "2026-05-09T22:15:00Z"
  }
}

// 响应:
{
  "risk_score": 82.5,
  "confidence": 0.93,
  "model": "xgboost_v2.3_industry_finance",
  "top_factors": [
    {"feature": "doc_dest_external", "contribution": 0.35},
    {"feature": "time_after_hours", "contribution": 0.28},
    {"feature": "doc_size_large", "contribution": 0.15}
  ],
  "recommended_action": "block_and_alert",
  "explanation": "用户在工作时间外向可移动设备复制大文件(50MB), 结合该用户近3天USB操作量异常增长400%"
}
```

```json
// POST /api/ai/feedback
// 请求: 人工标注
{
  "alert_id": "ALT-20260509-0042",
  "label": "true_positive",
  "category": "data_exfiltration",
  "severity": 4,
  "reviewer": "admin",
  "notes": "确认是离职员工窃取客户数据",
  "correct_action": "block"
}
```

---

## 六、模型训练与部署

### 6.1 训练流水线

```yaml
训练策略:
  - 初始训练: 积累 500+ 标注样本后首次训练
  - 定期重训练: 每周日 02:00 自动重训练
  - 增量更新: 新标注样本超过 200 条时触发增量训练
  - A/B 测试: 新模型先在影子模式运行 3 天, 指标优于旧模型后切换

数据划分:
  - 训练集: 70% (包含人工标注 + 高置信度自动标注)
  - 验证集: 15% (用于超参调优)
  - 测试集: 15% (最终评估, 严格隔离)

模型存储:
  - MLflow Model Registry
  - 版本管理: v{major}.{minor}.{patch}
  - 每个模型保存: 模型文件 + 特征定义 + 训练参数 + 评估指标
```

### 6.2 推理部署

```
推理服务架构:

  Nginx (负载均衡)
     │
     ├── AI-Inference-1 (GPU) ─── 模型缓存 ─── Redis
     ├── AI-Inference-2 (GPU) ─── 模型缓存 ─── Redis
     └── AI-Inference-N (CPU fallback)

  每个推理实例:
    - 预加载最新模型到内存
    - 支持批量推理 (batch_size=32)
    - P99延迟 < 50ms (无监督模型) / < 100ms (有监督模型)
    - 模型热切换 (不中断服务)
```

---

## 七、行业自适应

### 7.1 行业特征权重矩阵

| 特征维度 | 金融 | 制造 | 科技 | 政府 |
|---------|------|------|------|------|
| 邮件外部发送 | **0.20** | 0.10 | 0.08 | 0.15 |
| USB操作 | **0.15** | **0.20** | 0.10 | **0.25** |
| 打印 | 0.05 | **0.15** | 0.05 | **0.20** |
| 云盘/网页上传 | 0.10 | 0.08 | **0.20** | 0.05 |
| IM文件传输 | 0.08 | 0.05 | **0.15** | 0.05 |
| 代码/图纸操作 | 0.02 | **0.18** | **0.22** | 0.02 |
| 非工作时间 | **0.15** | **0.12** | **0.12** | **0.18** |
| 审批合规 | **0.15** | 0.05 | 0.03 | **0.08** |
| 加密/解密 | **0.10** | 0.07 | 0.05 | 0.02 |

### 7.2 行业模型自适应流程

```
1. 加载基础模型 (Base UEBA Model)
2. 根据行业模板加载特征权重矩阵
3. 读取该行业的历史标注数据
4. Fine-tune 最后3层 (迁移学习)
5. 验证集评估 → 达标后上线
6. 持续收集该行业的反馈数据
7. 每月行业模型独立重训练
```

---

## 八、可解释性

每个 AI 决策必须可解释：

```
告警详情中展示:
┌────────────────────────────────────────────┐
│ AI 风险评分: 82/100 (置信度 93%)             │
│ 模型: XGBoost v2.3 (金融行业)                │
│                                              │
│ 📊 主要影响因素 (SHAP值):                    │
│ ████████████████████ 目标路径异常 +35%       │
│ ██████████████ 非工作时间操作 +28%           │
│ ██████████ 文件大小异常 +15%                 │
│ ████████ USB操作量激增 +12%                  │
│ ██ 历史同类行为偏移 +8%                      │
│                                              │
│ 🔍 同类事件参考:                             │
│ 该用户90天内有3次类似行为                    │
│ 同类岗位用户均值为0.2次/月                   │
│                                              │
│ 💡 AI建议: 立即阻断 + 通知主管 + 启动审计     │
└────────────────────────────────────────────┘
```

---

## 九、实施路线图

```
Phase 1: AI基础 (第1-2周)
├── 特征工程模块 (63维特征提取)
├── Isolation Forest 无监督模型
├── 基础评分 API
└── 标注数据库 schema

Phase 2: 有监督学习 (第3-4周)
├── XGBoost 分类器
├── 人工标注界面
├── 反馈闭环
└── 模型评估报告

Phase 3: 深度学习 (第5-8周)
├── LSTM 时序模型
├── AutoEncoder 异常检测
├── SHAP 可解释性
└── 模型 A/B 测试

Phase 4: 行业自适应 (第9-12周)
├── 4套行业基础模型
├── 迁移学习 pipeline
├── 行业特征权重自动优化
└── 模型效果对比报告
```
