# IP-Guard UEBA AI 模型选型推荐

> API vs 本地部署 | 大模型 vs 小模型 | 推荐方案 & 成本分析

---

## 一、核心结论：分层模型架构

**不要用单一模型解决所有问题。DLP/UEBA 场景需要分层模型策略：**

```
层级          模型类型              延迟要求    部署方式      成本
─────────────────────────────────────────────────────────────────
L1 实时检测    传统 ML (XGBoost)    <10ms      本地 CPU       零
L2 内容分析    小模型 (1B-7B)       <100ms     本地 GPU       低
L3 综合研判    中模型 (14B-32B)     <500ms     本地 GPU/API   中
L4 报告生成    大模型 (API)         <5s        API 调用       按量
```

**关键原则：L1 处理 95% 的事件，L2 处理 4%，L3 处理 1%，不要让大模型处理所有事件。**

---

## 二、各层具体推荐

### L1: 实时异常检测 → 传统 ML（本地 CPU）

```
推荐：XGBoost + Isolation Forest + DBSCAN
理由：
  - P99 延迟 < 5ms，单机可处理 10000+ 事件/秒
  - 可解释性强（SHAP 值），满足合规要求
  - 零硬件成本，普通服务器即可
  - 我们已经在 engine/ai/model_trainer.py 中实现了 Isolation Forest
  - XGBoost 通过 pip install xgboost 即可集成

不推荐大模型做 L1 的原因：
  - 14B 模型单次推理至少 200ms-1s
  - 需要 GPU，成本高
  - 对于"用户复制了500个文件到U盘"这种事，不需要大模型判断
```

### L2: 内容敏感度分析 → 小模型（本地 GPU 可选）

```
推荐方案 A（首选）：Qwen2.5-7B-Instruct + LoRA 微调
  - 显存需求: 16GB (FP16) / 8GB (INT4量化)
  - 推理速度: ~50 tokens/s (单张 RTX 3060)
  - 微调成本: 1000条标注数据 + 1小时训练
  - 用途: 判断"这封邮件的正文是否包含敏感报价信息"

推荐方案 B（备选）：BERT-base 中文
  - 显存需求: 2GB
  - 推理速度: <10ms
  - 用途: 敏感文本分类（比大模型快但理解力较弱）

推荐方案 C（轻量）：text2vec-large-chinese
  - 纯 CPU 可运行
  - 用途: 文件内容向量化 + 相似度匹配
```

### L3: 综合风险研判 → 中模型（本地 GPU）

```
推荐：Qwen2.5-14B-Instruct / Qwen2.5-32B-Instruct

场景：当一个用户同时触发了以下告警时：
  - USB 批量写入
  - 邮件发送给外部
  - 非工作时间操作
  - 最近有解密行为

需要模型综合判断："这是离职前数据窃取" vs "这是正常的项目交付"

本地部署方案：
┌────────────────────────────────────────────────────┐
│ 模型            显存需求    推荐 GPU      推理速度   │
├────────────────────────────────────────────────────┤
│ Qwen2.5-14B    28GB FP16   RTX 4090 24G  30 t/s  │
│ Qwen2.5-14B    8GB INT4    RTX 3060 12G  25 t/s  │
│ Qwen2.5-32B    64GB FP16   A100 80G      20 t/s  │
│ Qwen2.5-32B    16GB INT4   RTX 4090 24G  15 t/s  │
│ DeepSeek-14B   28GB FP16   RTX 4090 24G  35 t/s  │
└────────────────────────────────────────────────────┘

对于 UEBA 场景，14B INT4 量化版本是最佳平衡点：
  - RTX 3060 12G 消费级显卡即可运行 (约 2000 元)
  - 25 tokens/s 对于日研判 100-500 个复杂事件足够
  - 相比 API 调用，无数据外泄风险
```

### L4: 报告生成 → API 调用（按需）

```
推荐：API 调用（通义千问 / DeepSeek / Claude API）

用途：
  - 周报/月报自然语言总结
  - 告警事件调查报告生成
  - 行业合规建议

成本：每月约 50-200 元（按实际调用量）
```

---

## 三、关于 A35B / 更大模型

```
你提到的 A35B 可能是指某些 35B 级别模型。

35B+ 模型的问题：
  ✗ 需要至少 A100 80G 或双卡
  ✗ 推理延迟 > 1s，不适合实时
  ✗ 微调成本高（需要 LoRA + 大量显存）
  ✗ 对于 DLP 分类任务，14B 已经足够

35B 模型的使用场景：
  ✓ 离线批量分析（夜间跑批）
  ✓ 复杂报告生成
  ✓ 如果你已有 A100/H100 硬件

建议：14B 做日常推理，35B 只在需要深度分析时调用
```

---

## 四、微调方案对比

### 方案A：全量微调 (Full Fine-tuning)

```
优点: 效果最好
缺点: 需要 60GB+ 显存 (14B模型), 训练时间长
适用: 有 A100/H100 的企业
不推荐: 成本太高
```

### 方案B：LoRA 微调 (推荐)

```
原理: 只训练少量参数（<1%），冻结原模型
显存: 14B 模型 + LoRA ≈ 20GB (FP16)
训练速度: 1000条数据约 30 分钟 (RTX 4090)
效果: 接近全量微调的 95%+

推荐配置:
  - 基座: Qwen2.5-14B-Instruct
  - 方法: QLoRA (4bit量化 + LoRA)
  - 显存: 12GB 即可 (RTX 3060)
  - 工具: LLaMA-Factory / Unsloth
```

### 方案C：RAG 增强 (零训练)

```
原理: 不修改模型，用向量数据库存储历史案例
优点: 零训练成本，实时更新
缺点: 对复杂推理帮助有限
适用: 快速上线阶段
```

---

## 五、推荐实施路径

```
Phase 1 (当前 - 第1周): 传统 ML 底座
├── Isolation Forest (已实现在 model_trainer.py)
├── XGBoost 分类器 (pip install xgboost)
├── 特征工程 (63维，已实现在 feature_extractor.py)
└── 反馈闭环 (已实现在 label_manager.py)

Phase 2 (第2-3周): 小模型接入
├── 部署 Qwen2.5-7B-Instruct (INT4量化)
├── LoRA 微调: DLP敏感内容识别
├── 接入 L2 内容分析链路
└── API 接口对接

Phase 3 (第4-6周): 中模型综合研判
├── 部署 Qwen2.5-14B-Instruct (INT4量化)
├── LoRA 微调: 综合风险研判
├── 接入 L3 复杂事件分析
└── 模型 A/B 测试

Phase 4 (第7-8周): 报告生成
├── API 对接（通义千问 / DeepSeek）
├── 自动周报/月报生成
└── 行业合规建议生成
```

---

## 六、硬件需求总览

| 规模 | 硬件 | 能跑的模型 | 预算 |
|------|------|-----------|------|
| 最小（POC） | CPU 16核 + 32GB RAM | XGBoost + IForest + 7B INT4 | 已有服务器 |
| 标准 | + RTX 3060 12G | + 14B INT4 LoRA | +2000元 |
| 推荐 | + RTX 4090 24G | + 14B FP16 + 32B INT4 | +15000元 |
| 企业 | + A100 80G | + 全量微调 14B/32B | +80000元 |

---

## 七、最终推荐

```
┌─────────────────────────────────────────────────────────┐
│                                                         │
│  首选方案: 传统 ML (本地CPU) + 14B INT4 (本地GPU)        │
│                                                         │
│  - 95% 事件: XGBoost/IForest 毫秒级处理                  │
│  - 4% 复杂事件: 7B 小模型内容分析                        │
│  - 1% 综合研判: 14B 中模型推理                           │
│  - 报告生成: API 按需调用                                │
│                                                         │
│  硬件: 普通服务器 + 1张 RTX 3060 (约2000元)              │
│  模型: Qwen2.5-14B-Instruct (INT4量化 + LoRA微调)        │
│  数据不出内网，安全合规                                  │
│                                                         │
└─────────────────────────────────────────────────────────┘
```
