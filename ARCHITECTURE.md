# IP-Guard UEBA 生产级架构规划

> 目标：支持万级终端、高并发、高可用、毫秒级响应、纯本地部署

---

## 一、容量预估

| 指标 | 1000终端 | 10000终端 | 50000终端 |
|------|----------|-----------|-----------|
| 日均日志量 | ~100万条 | ~1000万条 | ~5000万条 |
| 日均数据量 | ~500MB | ~5GB | ~25GB |
| 年存储量 | ~180GB | ~1.8TB | ~9TB |
| 并发查询 | 5-10 QPS | 50-100 QPS | 200+ QPS |
| 告警实时性 | 分钟级 | 分钟级 | 秒级/分钟级 |

---

## 二、整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    IP-Guard 源层 (不变)                       │
│  ┌──────────┐    ┌──────────────────────────────────┐       │
│  │ OCULAR3  │    │  OCULAR3_DATA.20260501           │       │
│  │ (主配置)  │    │  OCULAR3_DATA.20260502  ...      │       │
│  └──────────┘    └──────────────────────────────────┘       │
│       SQL Server (只读: ipguard_reader)                     │
└──────────────────────┬──────────────────────────────────────┘
                       │ CDC / 增量同步
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                     数据同步层                                │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │ Debezium    │  │ 定时SQL导出   │  │ Logstash/jdbc    │   │
│  │ (CDC实时)   │  │ (T+1批量)    │  │ (可选)           │   │
│  └──────┬──────┘  └──────┬───────┘  └──────┬───────────┘   │
│         └────────────────┼─────────────────┘               │
│                          ▼                                  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Apache Kafka / Redpanda                   │   │
│  │           (消息队列 - 削峰填谷, 异步解耦)               │   │
│  └──────────────────────┬───────────────────────────────┘   │
└─────────────────────────┼───────────────────────────────────┘
                          │
         ┌────────────────┼────────────────┐
         ▼                ▼                ▼
┌─────────────┐  ┌──────────────┐  ┌──────────────┐
│ 实时分析引擎  │  │ 批量预处理    │  │ 告警引擎      │
│ (Flink/Spark)│  │ (Celery任务) │  │ (规则+ML)    │
└──────┬──────┘  └──────┬───────┘  └──────┬───────┘
       │                │                 │
       └────────────────┼─────────────────┘
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                     存储层 (双引擎)                            │
│  ┌──────────────────────┐  ┌────────────────────────────┐   │
│  │    PostgreSQL 14+     │  │   ClickHouse / TimescaleDB  │   │
│  │  (配置/用户/策略/告警)  │  │   (日志时序数据 - 亿级)      │   │
│  │  - 热数据 (<30天)     │  │  - 冷热分层                 │   │
│  │  - 主键/外键关系      │  │  - 列存压缩(10:1)           │   │
│  │  - 事务一致性         │  │  - 物化视图/预聚合           │   │
│  └──────────────────────┘  └────────────────────────────┘   │
│  ┌──────────────────────┐  ┌────────────────────────────┐   │
│  │      Redis 7          │  │     MinIO (可选)           │   │
│  │  - API缓存 (热点数据)  │  │  - 截图/附件存储            │   │
│  │  - 实时计数器          │  │  - 报表文件                 │   │
│  │  - 分布式锁/任务队列   │  │  - S3兼容, 本地部署         │   │
│  └──────────────────────┘  └────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                      API 服务层                               │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌──────────┐ │
│  │FastAPI │ │FastAPI │ │FastAPI │ │FastAPI │ │WebSocket │ │
│  │实例 1  │ │实例 2  │ │实例 3  │ │实例 N  │ │实时推送   │ │
│  └────┬───┘ └───┬────┘ └───┬────┘ └───┬────┘ └────┬─────┘ │
│       └──────────┴─────────┴──────────┴───────────┘        │
│                          │                                   │
│              ┌───────────┴───────────┐                      │
│              │    Nginx / Traefik    │                      │
│              │  (负载均衡 + 限流 + SSL)│                     │
│              └───────────────────────┘                      │
└─────────────────────────────────────────────────────────────┘
                        │
                        ▼
┌─────────────────────────────────────────────────────────────┐
│                     前端层                                    │
│  ┌──────────────────────────────────────────────────────┐   │
│  │           React 18 SPA (Nginx 静态托管)               │   │
│  │  - Ant Design Pro / shadcn/ui                         │   │
│  │  - 仪表盘 / 行为分析 / 告警 / 报表 / 策略管理           │   │
│  │  - ECharts 大屏可视化                                  │   │
│  │  - WebSocket 实时告警推送                              │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## 三、关键技术选型

### 3.1 存储层

| 组件 | 选型 | 理由 |
|------|------|------|
| 配置/用户/告警 | **PostgreSQL 14+** | 事务支持、JSONB、窗口函数、丰富索引(GIN/GiST/BRIN)、成熟生态 |
| 日志时序数据 | **ClickHouse** | 列存压缩比10:1、亿级数据亚秒查询、物化视图自动预聚合、支持SQL |
| 备选时序 | TimescaleDB | PostgreSQL插件、自动分区、持续聚合、运维简单(与PG统一) |
| 缓存 | **Redis 7** | 亚毫秒响应、丰富数据结构(Sorted Set做排行榜)、Stream做轻量队列 |

### 3.2 为什么是 ClickHouse 而非 Elasticsearch

| 维度 | ClickHouse | Elasticsearch |
|------|------------|---------------|
| 写入性能 | 百万行/秒 | 数万行/秒 |
| 存储压缩 | 10:1 | 2:1 |
| 聚合查询 | 原生OLAP引擎,亚秒 | 较慢,需大量内存 |
| SQL支持 | 完整SQL | DSL(学习成本) |
| 运维复杂度 | 低(单进程) | 高(集群调优) |
| 本地部署 | Docker单机即用 | 需调JVM |

### 3.3 API 层

| 组件 | 选型 | 理由 |
|------|------|------|
| 框架 | **FastAPI** | 异步(uvloop)、自动OpenAPI文档、Pydantic验证、Python生态 |
| ASGI | **Uvicorn** + **Gunicorn** | 多worker多线程、优雅重启 |
| 连接池 | SQLAlchemy 2.0 async + asyncpg | 异步数据库驱动,连接池复用 |
| 缓存策略 | Redis + 内存LRU | 热点数据内存缓存,次热数据Redis |

### 3.4 前端

| 组件 | 选型 | 理由 |
|------|------|------|
| 框架 | **React 18** + TypeScript | 生态最丰富 |
| 组件库 | **Ant Design 5** | 企业级后台,表格/表单/图表丰富 |
| 图表 | **ECharts 5** | 大数据量渲染性能好,大屏支持 |
| 构建 | **Vite 5** | 秒级热更新,构建快 |
| 状态管理 | Zustand / Jotai | 轻量,适合中等复杂度 |

---

## 四、数据同步策略

### 4.1 方案对比

| 方案 | 实时性 | 复杂度 | 对源库影响 | 推荐场景 |
|------|--------|--------|-----------|----------|
| **CDC (Debezium + Kafka)** | 秒级 | 高 | 低(读事务日志) | 生产环境,万级终端 |
| **定时批量导出 (Python)** | T+1 | 低 | 中(SELECT查询) | 小规模,POC |
| **触发器推送** | 实时 | 中 | 高(影响写入) | 不推荐 |

### 4.2 推荐同步策略

```
阶段1 (当前POC):
  每天凌晨3点 → Python脚本 → SELECT增量 → SQLite/PostgreSQL
  
阶段2 (生产部署):
  Debezium CDC → Kafka → Flink消费 → ClickHouse(实时) + PostgreSQL(配置)
  
  - 配置表(USER, AGENT, POLICY等): 全量同步,每小时
  - 日志表(OCULAR3_DATA.*): CDC实时增量
  - 新日志库(每天新建): 自动发现并注册到同步任务
```

---

## 五、性能优化策略

### 5.1 数据库层

```
ClickHouse 优化:
  - 按日期分区 (PARTITION BY toYYYYMMDD(time))
  - 按用户ID排序键 (ORDER BY user_id, time)
  - 物化视图预聚合: 每小时/每天/每用户 行为统计
  - TTL自动淘汰: 热数据30天SSD, 温数据90天HDD, 冷数据1年归档

PostgreSQL 优化:
  - 连接池: PgBouncer (transaction模式)
  - 索引: 复合索引 + 部分索引 + BRIN索引(大表)
  - 分区: 按日期范围分区告警表
  - 读写分离: 主库写入, 只读副本查询
```

### 5.2 API 层

```
响应时间目标:
  - 仪表盘概览: <100ms (Redis缓存)
  - 用户行为查询: <200ms (ClickHouse)
  - 告警列表: <150ms (PostgreSQL + Redis)
  - 报表导出: <5s (异步任务, 流式返回)

优化手段:
  1. Redis缓存热点查询 (5分钟TTL)
  2. 预计算每日聚合表 (凌晨跑批)
  3. 分页 + 游标 (避免OFFSET大翻页)
  4. 异步导出 (Celery任务 + WebSocket进度推送)
  5. Nginx gzip + 静态资源CDN
```

### 5.3 前端层

```
- 虚拟滚动 (react-window): 万行表格流畅滚动
- 懒加载: 路由级代码分割
- 图表降采样: ECharts dataZoom + 服务端聚合
- 骨架屏: 感知性能优化
```

---

## 六、高可用设计

```
┌─────────────────────────────────────────────────┐
│                Nginx (active)                    │
│          health_check + fail_timeout             │
└────────┬────────────────────┬───────────────────┘
         │                    │
    ┌────▼────┐          ┌────▼────┐
    │FastAPI-1│          │FastAPI-2│   (多实例)
    │ :8001   │          │ :8002   │
    └────┬────┘          └────┬────┘
         │                    │
    ┌────▼────────────────────▼────┐
    │        PgBouncer              │  (连接池)
    │     (transaction pooling)     │
    └────────┬──────────┬──────────┘
             │          │
    ┌────────▼──┐  ┌───▼──────────┐
    │ PostgreSQL │  │  PostgreSQL  │  (主从复制)
    │  (master)  │──│  (replica)   │
    └────────────┘  └──────────────┘
             │
    ┌────────▼──┐  ┌──────────────┐
    │ Redis     │  │  ClickHouse  │  (集群/副本)
    │ Sentinel  │  │  Replicated  │
    └───────────┘  └──────────────┘
```

**容灾等级:**
- 单机Docker: 适合POC/小规模, 无HA
- 双机HA: PostgreSQL主从 + Redis Sentinel + FastAPI多实例
- 集群: K8s部署, 自动故障转移, 水平扩缩

---

## 七、本地部署方案 (Docker Compose)

### 7.1 小规模 (单机, <5000终端)

```yaml
# docker-compose.prod.yml
services:
  postgres:
    image: postgres:15-alpine
    volumes: [pgdata:/var/lib/postgresql/data]
    
  clickhouse:
    image: clickhouse/clickhouse-server:latest
    volumes: [chdata:/var/lib/clickhouse]
    
  redis:
    image: redis:7-alpine
    
  backend:
    build: ./backend
    deploy: {replicas: 2}
    depends_on: [postgres, clickhouse, redis]
    
  frontend:
    image: nginx:alpine
    volumes: [./frontend/dist:/usr/share/nginx/html]
    
  nginx:
    image: nginx:alpine
    ports: ["80:80"]
    volumes: [./nginx.conf:/etc/nginx/nginx.conf]
```

### 7.2 中大规模 (K8s / Docker Swarm)

```
- PostgreSQL: Patroni HA (3节点) + etcd
- ClickHouse: 3节点集群 (1 shard × 3 replica)
- Redis: Sentinel (3节点)
- FastAPI: HPA自动扩缩 (2-10副本)
- Kafka: 3节点 (或Redpanda单机替代)
- 监控: Prometheus + Grafana + Loki
```

---

## 八、实施路线图

```
Phase 1 (当前 - POC验证)          Phase 2 (MVP)              Phase 3 (生产)
┌─────────────────────┐    ┌─────────────────────┐    ┌─────────────────────┐
│ ✓ SQLite 单机       │───▶│ PostgreSQL + Redis   │───▶│ ClickHouse + Kafka   │
│ ✓ Python stdlib API │    │ FastAPI + Gunicorn   │    │ Flink实时分析        │
│ ✓ 静态HTML Dashboard│    │ React + Ant Design   │    │ ML异常检测           │
│ ✓ 数据字典          │    │ React + Ant Design   │    │ 大屏可视化           │
│                     │    │ 定时增量同步          │    │ K8s高可用部署        │
└─────────────────────┘    └─────────────────────┘    └─────────────────────┘
      本周完成                  2-3周                      4-8周
```

---

## 九、技术栈总览

| 层级 | 组件 | 小规模(POC) | 生产环境 |
|------|------|-------------|----------|
| 数据源 | SQL Server | 只读连接 | CDC |
| 消息队列 | - | ❌ | Kafka/Redpanda |
| 配置库 | PostgreSQL | ✅ SQLite替代 | ✅ |
| 日志库 | ClickHouse | ❌ (用PG) | ✅ |
| 缓存 | Redis | ❌ (可省略) | ✅ |
| API | FastAPI | ✅ | ✅ + Gunicorn |
| 前端 | React SPA | ✅ Vite | ✅ Nginx托管 |
| 反向代理 | Nginx | ✅ | ✅ |
| 部署 | Docker Compose | ✅ | Docker/K8s |
