# EDSP 今日讨论整理：从 Demo 骨架到可交付的数据安全告警运营平台

> 日期：2026-05-20  
> 项目：EDSP 企业数据安全分析平台  
> 主题：代码审查、产品化路线、数据库接入识别、多源接入、AI/Agent 能力、客户交付体验

---

## 目录

1. [今日讨论总览](#1-今日讨论总览)
2. [当前项目状态判断](#2-当前项目状态判断)
3. [初始代码审查结论](#3-初始代码审查结论)
4. [Demo 之后的产品化方向](#4-demo-之后的产品化方向)
5. [真实业务闭环设计](#5-真实业务闭环设计)
6. [不同厂家数据库接入的核心难点](#6-不同厂家数据库接入的核心难点)
7. [数据库智能识别引擎设计](#7-数据库智能识别引擎设计)
8. [模板库不是万能的：语义识别方案](#8-模板库不是万能的语义识别方案)
9. [扫描完整性核验与多 Agent 核验](#9-扫描完整性核验与多-agent-核验)
10. [AI 与本地小模型路线](#10-ai-与本地小模型路线)
11. [告警学习闭环与模型降噪](#11-告警学习闭环与模型降噪)
12. [两套模型体系：结构化风险模型 + 本地小语言模型](#12-两套模型体系结构化风险模型--本地小语言模型)
13. [客户交付体验：尽量减少人工操作](#13-客户交付体验尽量减少人工操作)
14. [多源接入方式设计](#14-多源接入方式设计)
15. [数据库识别问题的最终落地路线](#15-数据库识别问题的最终落地路线)
16. [建议下一步开发优先级](#16-建议下一步开发优先级)
17. [最终产品定位](#17-最终产品定位)

---

# 1. 今日讨论总览

今天主要围绕 EDSP 从 Demo 骨架走向产品化做了系统讨论。

核心问题包括：

```text
1. 当前代码是否能作为正式产品基础？
2. Demo 已跑通后，下一步应该做什么？
3. 不同厂家的数据库结构都不一样，如何识别哪些数据需要告警？
4. 如果数据库表很多，系统如何确认自己没有漏扫？
5. 后期是否需要本地小模型、本地 Agent？
6. 模型是否可以逐步学习哪些告警是真高危、哪些可以降噪？
7. 交付时如何减少客户人工操作，提高客户体验？
8. 除数据库和 Syslog 外，是否还需要其他接入方式？
9. 最终如何设计一个可落地、可核验、可学习的多源安全数据接入体系？
```

最终形成的核心共识是：

> **EDSP 不应该只是一个数据库读取工具，也不应该只做固定厂商适配，而应该成为一个多源安全事件接入、标准化、分析、告警、降噪和运营的平台。**

---

# 2. 当前项目状态判断

当前 EDSP 项目已经具备第一阶段基础骨架：

```text
- Spring Cloud 多模块后端
- PostgreSQL 初始表结构
- H2 本地开发库启动链路
- 数据源管理 CRUD
- SQL Server 数据源配置和连接测试
- 标准告警接入 API
- 告警幂等写入
- 通知中心基础链路
- Schema 表和字段映射占位 API
- 规则、告警、报表任务占位 API
- React + Ant Design 管理台
```

但当前更适合定位为：

```text
EDSP 第一阶段 Demo / POC 骨架
```

而不是：

```text
企业级可交付版本
```

后续重点不再是“Demo 能不能跑起来”，而是：

```text
真实数据能否接进来
告警能否闭环
规则能否配置
客户能否低成本使用
系统能否持续学习
不同厂家能否逐步适配
```

---

# 3. 初始代码审查结论

## 3.1 高风险问题

初始审查中重点发现：

```text
1. 认证只是 Demo 逻辑
2. 默认账号密码风险较高
3. Token 是 Base64 字符串，不是真正 JWT
4. 网关层没有看到统一鉴权
5. 数据源 configJson 可能明文保存数据库账号密码
6. Webhook URL 没有限制，存在 SSRF 风险
7. 告警接入服务使用的字段与当前 migration 可能不完全一致
8. /sample 这类接口如果暴露，可能读取客户业务数据
```

## 3.2 安全以外的重点

虽然用户后续说明“不考虑安全性”，但代码审查仍然给出了一条重要判断：

```text
当前项目已经具备骨架价值；
接下来应该从 Demo 骨架进入产品化阶段。
```

---

# 4. Demo 之后的产品化方向

用户明确表示：

> Demo 已经跑起来了，现在不考虑 Demo 的事情了。

因此后续路线切换为：

```text
从“能不能运行”
转向“能不能真实接入、真实处置、真实交付”
```

产品化核心目标：

```text
真实数据源
  ↓
数据采集 / 推送接入
  ↓
字段标准化
  ↓
规则匹配
  ↓
生成告警
  ↓
通知负责人
  ↓
人工或自动处置
  ↓
处置结论
  ↓
报表 / 复盘 / 模型学习
```

---

# 5. 真实业务闭环设计

EDSP 后续要形成完整业务闭环：

```text
事件进入平台
  ↓
标准化成 standard_events
  ↓
规则 / 模型判断
  ↓
生成 alerts
  ↓
通知 / 分派 / 升级
  ↓
处置 / 误报 / 关闭
  ↓
处置结果回流
  ↓
规则优化 / 模型学习
```

## 5.1 告警中心

建议完善：

```text
- 告警列表
- 告警详情
- 告警状态流转
- 处置记录
- 通知记录
- 原始事件查看
- 关联事件查看
- 审计日志
```

## 5.2 告警状态建议

```text
open            新告警
assigned        已分派
processing      处理中
confirmed       已确认风险
false_positive  误报
resolved        已处置
closed          已关闭
```

## 5.3 报表和驾驶舱

首页建议展示：

```text
今日告警数
高危告警数
未处理告警数
接入系统数
通知成功率
近 7 天告警趋势
Top 风险用户
Top 高危终端
Top 命中规则
```

---

# 6. 不同厂家数据库接入的核心难点

用户重点担心：

> 每个厂家的数据库内容都不一样，标准也不一样，没有标准答案。

这是 EDSP 最大难点之一。

问题本质：

```text
1. 厂商数据库结构不公开
2. 同一厂商不同版本表结构可能不同
3. 同一厂商不同客户启用模块不同
4. 表名和字段名不统一
5. 有些库里是告警表，有些只是原始日志
6. 有些数据需要多表 join 才能理解
7. 厂家通常不会提供接口
8. 一体机甚至无法连接数据库
```

因此，不能设计成：

```text
预置所有厂家模板 → 自动识别所有数据库 → 直接准确告警
```

这不现实。

正确方向：

```text
可识别
可核验
可试运行
可修正
可沉淀
可复用
可学习
```

---

# 7. 数据库智能识别引擎设计

建议设计：

```text
Database Intelligence Engine
数据库智能识别引擎
```

它不是简单识别表名，而是把陌生数据库转换成统一的“数据库画像”。

## 7.1 数据库画像

不同数据库先统一成内部结构：

```text
catalog
schema
table
column
index
primary_key
foreign_key
row_count
sample_values
last_update_time
```

示例：

```json
{
  "databaseType": "sqlserver",
  "schemas": [
    {
      "name": "dbo",
      "tables": [
        {
          "name": "T_RISK_EVENT",
          "rowCount": 1200000,
          "columns": [
            {
              "name": "RISK_LEVEL",
              "type": "varchar",
              "samples": ["高危", "中危", "低危"],
              "nullable": false
            }
          ],
          "primaryKeys": ["ID"],
          "indexes": ["CREATE_TIME"],
          "foreignKeys": []
        }
      ]
    }
  ]
}
```

## 7.2 识别层级

建议分 7 层：

```text
1. 数据库元数据采集层
2. 数据库完整性核验层
3. 表角色识别层
4. 字段语义识别层
5. 多表关系识别层
6. 标准事件转换层
7. 证据链与置信度层
```

---

# 8. 模板库不是万能的：语义识别方案

用户提出：

> 如果用模板库，但每一家产品数据库里面的告警信息可能不一样，那怎么识别？

核心答案：

> **模板库不能写死字段名，而应该定义“告警语义槽位”。**

## 8.1 错误模板设计

```json
{
  "vendor": "某厂家",
  "table": "alarm_log",
  "fields": {
    "alarm_name": "title",
    "alarm_time": "occurredAt"
  }
}
```

这种太死。

## 8.2 正确模板设计

```json
{
  "templateName": "通用告警表模板",
  "tableNamePatterns": [
    "alarm",
    "alert",
    "warning",
    "risk",
    "violation",
    "event"
  ],
  "requiredSemanticFields": [
    "occurredAt",
    "title"
  ],
  "optionalSemanticFields": [
    "severity",
    "actor",
    "asset",
    "policyName",
    "status",
    "alertType"
  ],
  "fieldSemanticPatterns": {
    "occurredAt": ["time", "date", "occur", "created", "发生时间", "告警时间"],
    "title": ["title", "name", "msg", "message", "desc", "告警名称", "事件描述"],
    "severity": ["level", "severity", "grade", "risk", "priority", "等级", "级别"],
    "actor": ["user", "account", "operator", "employee", "用户", "账号", "操作人"],
    "asset": ["host", "computer", "endpoint", "device", "ip", "主机", "终端"]
  }
}
```

## 8.3 标准告警槽位

不管厂家字段叫什么，都要识别是否表达这些语义：

```text
告警ID
告警标题
告警等级
告警类型
发生时间
操作用户
终端资产
策略名称
处理状态
告警详情
原始记录ID
```

## 8.4 字段语义识别依据

```text
字段名
字段类型
样本值
数据分布
唯一性
是否可为空
是否像时间
是否像等级
是否像用户名
是否像文件路径
是否像 IP/MAC
是否与其他表有关联
```

---

# 9. 扫描完整性核验与多 Agent 核验

用户提出非常关键的问题：

> 比如 2000 个数据库表，万一只扫描了 200 个怎么办？

这说明 EDSP 需要的不只是自动识别，还需要：

```text
扫描完整性核验能力
接入审计能力
```

## 9.1 自动识别与核验要分开

```text
识别引擎：判断哪些表像告警表/日志表
核验引擎：判断扫描过程是否完整、可信、可追溯
```

## 9.2 扫描结果不能只显示“完成”

真正成功应该显示：

```text
数据库实际表数：2000
扫描发现表数：1995
成功分析表数：1980
失败表数：15
覆盖率：99.75%
核验状态：通过 / 警告 / 失败
```

## 9.3 建议新增 scan session

```sql
create table if not exists schema_scan_runs (
    id bigserial primary key,
    data_source_id bigint not null,
    scan_type varchar(40) not null default 'full',
    status varchar(40) not null default 'running',

    expected_table_count int,
    discovered_table_count int,
    analyzed_table_count int,
    failed_table_count int,
    ignored_table_count int,

    coverage_rate numeric(5,2),
    verification_status varchar(40),

    started_at timestamptz not null default now(),
    finished_at timestamptz,
    error_message text
);
```

## 9.4 核验类型

```text
1. 总量核验
2. 分页核验
3. 权限核验
4. Schema 范围核验
5. 表结构快照核验
```

## 9.5 后期多 Agent 分工

```text
Agent 1：Schema Scanner
负责扫描数据库表、字段、样例值

Agent 2：Coverage Verifier
负责核验表数量、schema 范围、扫描完整性

Agent 3：Semantic Mapper
负责字段语义识别和标准字段映射

Agent 4：Template Matcher
负责匹配厂商模板、场景模板、通用模板

Agent 5：Risk Rule Advisor
负责建议绑定哪些规则模板

Agent 6：Audit Reviewer
负责复核扫描报告，判断是否可以正式接入
```

---

# 10. AI 与本地小模型路线

用户提出：

> 本地也需要训练一个本地大模型，可能 0.7B 或 1B 左右就够了。

判断：

> 0.7B / 1B 小模型够用，但它不应该作为全能大脑，而应作为本地语义识别与核验 Agent。

## 10.1 适合本地小模型做的事情

```text
1. 判断字段语义
2. 判断表是否像告警表 / 日志表 / 用户表 / 配置表
3. 根据字段样例推断字段含义
4. 给模板匹配结果打辅助分
5. 解释为什么某张表被识别为告警表
6. 核验扫描报告是否异常
7. 发现“只扫了 200 张但实际有 2000 张”的异常
8. 生成人工复核建议
9. 生成接入报告说明
```

## 10.2 不建议从零训练

建议：

```text
下载开源小模型
  ↓
准备 EDSP 领域数据
  ↓
做 LoRA / QLoRA 微调
  ↓
得到领域适配模型
  ↓
本地部署推理
```

## 10.3 训练数据来源

主要来自：

```text
字段语义识别样本
表类型识别样本
扫描核验样本
人工修正记录
模板匹配结果
告警处置结果
误报/真风险标签
```

---

# 11. 告警学习闭环与模型降噪

用户提出：

> 后续可以让模型识别告警，前期可能 100% 全部告警，经过模型学习分析后逐渐知道哪些需要告警，哪些不是高危，哪些需要立即处理。

这是 EDSP 后期的重要能力。

## 11.1 演进路线

```text
前期：全部接入 / 规则粗筛 / 人工处置
中期：模型辅助分级 / 降噪 / 推荐处理优先级
后期：模型自动判断高危 / 中危 / 低危 / 可忽略
```

## 11.2 告警学习闭环

```text
所有标准事件 / 厂商告警
  ↓
先全部进入平台
  ↓
规则初步判断
  ↓
模型辅助评分
  ↓
生成告警等级和处理优先级
  ↓
人工处置
  ↓
处置结果回流
  ↓
模型学习
  ↓
下一次判断更准确
```

## 11.3 告警处理分层

建议模型输出不只是“告警/不告警”，而是四类：

| 模型判断 | 处理方式 |
|---|---|
| `immediate_alert` | 立即告警，马上通知 |
| `normal_alert` | 普通告警，进入告警中心 |
| `low_priority` | 低优先级，只汇总不打扰 |
| `archive_only` | 仅记录事件，不生成告警 |

## 11.4 最终效果

```text
前期：
1000 条告警 → 人工看 1000 条

中期：
1000 条告警 → 人工优先看前 200 条

后期：
1000 条告警 →
  50 条立即处理
  150 条普通处理
  300 条汇总观察
  500 条自动归档
```

---

# 12. 两套模型体系：结构化风险模型 + 本地小语言模型

用户问：

> 结构化风险评分：XGBoost / LightGBM  
> 文本语义理解：本地 0.7B / 1B 小模型  
> 异常检测：Isolation Forest / AutoEncoder  
> 解释和报告：本地小模型  
> 这个是两套模型是吧？

结论：

> 是的，更准确地说是两大类能力：  
> **结构化风险评分模型 + 本地小语言模型。**

## 12.1 第一套：结构化风险评分模型

适合：

```text
XGBoost
LightGBM
Isolation Forest
AutoEncoder
Logistic Regression
```

负责：

```text
这个事件风险分是多少？
这个告警是真风险还是误报？
这个告警是 high / medium / low？
这个用户行为是否异常？
这个告警是否需要立即处理？
```

## 12.2 第二套：本地小语言模型

适合：

```text
0.7B / 1B 小模型
Qwen 小模型
其他本地轻量模型
```

负责：

```text
这个字段是什么意思？
这张表像不像告警表？
这个告警描述表达的风险是什么？
为什么这个告警被判为高危？
怎么给管理员解释？
怎么生成处置建议？
怎么生成扫描核验报告？
```

## 12.3 简单理解

```text
结构化模型：负责“算分”
本地小模型：负责“理解和解释”
```

---

# 13. 客户交付体验：尽量减少人工操作

用户提出：

> 真正交付的时候尽量减少或者不用客户人工处置的操作，尽可能提高客户体验。

核心结论：

> 客户不是实施工程师，不能让客户一张表一张表确认、一个字段一个字段映射、天天处理大量低价值告警。

## 13.1 托管接入模式

建议提供两种模式：

| 模式 | 面向对象 | 特点 |
|---|---|---|
| 专家模式 | 实施工程师 / 高级管理员 | 可以看表、字段、映射、规则 |
| 托管模式 | 客户安全管理员 / 普通使用者 | 只看结果、建议和确认按钮 |

客户默认使用托管模式。

## 13.2 客户只确认业务策略

不要让客户确认：

```text
alarm_time → occurredAt
user_name → actor
risk_level → severity
```

客户应该确认：

```text
是否启用文件外发告警？
是否启用 U 盘拷贝告警？
是否启用邮件外发告警？
是否启用非工作时间操作告警？
是否高危立即通知？
```

## 13.3 客户日常只看驾驶舱

客户首页应该显示：

```text
今日需要处理
今日已自动归档
高危趋势
风险用户排行
接入健康状态
系统建议
```

而不是：

```text
表字段映射
规则表达式
同步任务参数
扫描日志明细
```

---

# 14. 多源接入方式设计

用户后续明确：

> 不仅仅是数据库和 Syslog，还有其他能接入进来的方式都可以。

因此 EDSP 应该是多源接入平台。

## 14.1 建议支持 8 类接入方式

```text
1. 数据库接入
2. Syslog 接入
3. HTTP API / Webhook 接入
4. CSV / Excel / JSON / XML 文件导入
5. SFTP / FTP / SMB / 共享目录采集
6. Agent / Collector 采集
7. 消息队列接入
8. 日志平台 / SIEM / 大数据平台接入
```

## 14.2 Syslog 是一等公民

因为很多一体机：

```text
不给接口
不给数据库
只能发 Syslog
```

所以 EDSP 要支持：

```text
UDP Syslog
TCP Syslog
TLS Syslog
CEF
LEEF
自定义文本日志
```

## 14.3 统一内部链路

不管来源是什么，都统一到：

```text
数据库
Syslog
Webhook
API
CSV / Excel
SFTP 文件
Agent
Kafka
日志平台
  ↓
raw_data 原始数据层
  ↓
parser / mapper 解析映射层
  ↓
standard_events 标准事件层
  ↓
rules / risk model 规则和风险模型
  ↓
alerts 标准告警层
  ↓
notification / report 通知和报表
```

---

# 15. 数据库识别问题的最终落地路线

今天讨论最终回到核心：

> 不同库怎么识别？

最终建议不是押注单一方案，而是组合：

```text
统一数据库画像
  +
表角色分类
  +
字段语义识别
  +
多表关系推断
  +
模板匹配
  +
样本反向验证
  +
置信度分级
  +
试运行沙箱
  +
人工/Agent 修正
  +
模板沉淀
```

## 15.1 六个核心模块

```text
1. Database Inventory
   数据库完整画像采集

2. Coverage Verification
   扫描完整性核验

3. Semantic Profiler
   表/字段语义画像

4. Template Matcher
   语义模板匹配

5. Ingestion Plan Generator
   接入方案生成器

6. Shadow Validator
   试运行验证器
```

## 15.2 接入方案生成器示例

```json
{
  "dataSourceId": 1,
  "planName": "某厂家 DLP 告警接入方案",
  "mode": "alert_table",
  "confidence": 92,
  "mainTable": "T_RISK_EVENT",
  "joinTables": [
    "T_RISK_DETAIL",
    "T_USER_INFO"
  ],
  "cursorField": "CREATE_TIME",
  "idField": "ID",
  "fieldMappings": {
    "ID": "externalId",
    "RISK_NAME": "title",
    "RISK_LEVEL": "severity",
    "CREATE_TIME": "occurredAt",
    "USER_ACCOUNT": "actor",
    "HOST_NAME": "asset"
  },
  "syncStrategy": {
    "type": "incremental",
    "cursor": "CREATE_TIME",
    "interval": "5m"
  },
  "risk": [
    "RISK_LEVEL 字段样本较少，建议试运行观察",
    "未发现处置状态字段，默认状态 open"
  ]
}
```

## 15.3 反向验证

系统识别后，要拿真实样本校验：

```text
occurredAt 是否都能解析成时间
severity 是否都能映射成 high / medium / low
title 是否为空
externalId 是否唯一
actor 是否大量为空
增量字段是否递增
重复率是否异常
```

---

# 16. 建议下一步开发优先级

结合今天讨论，建议下一步按这个优先级推进。

## 第一优先级：统一中间层

```text
1. 设计 standard_events 表
2. 设计 raw_data / raw_logs / raw_imports 原始层
3. 明确 alerts 只存真正需要关注的告警
4. 明确所有来源先进入 raw，再进入 standard_events，再进入 alerts
```

## 第二优先级：数据库识别 MVP

```text
1. Database Inventory：扫描库/表/字段/样本
2. Coverage Verification：核验是否扫全
3. Semantic Profiler：表类型和字段语义识别
4. Template Matcher：通用模板匹配
5. Ingestion Plan Generator：生成接入方案
6. Shadow Validator：试运行验证
```

## 第三优先级：Syslog Collector

```text
1. 支持 UDP/TCP Syslog 接收
2. 原始日志入 raw_logs
3. 支持 JSON / key=value / regex parser
4. 解析成 standard_events
5. 解析失败进入未知日志池
```

## 第四优先级：文件导入

```text
1. CSV / Excel 上传
2. 自动识别列
3. 自动字段映射
4. 预览导入结果
5. 入 standard_events / alerts
```

## 第五优先级：模型辅助

```text
1. 先用规则 + prompt，不急着训练
2. 保存人工修正样本
3. 后期 LoRA 微调小模型
4. 接入结构化风险评分模型
5. 做告警降噪和优先级排序
```

---

# 17. 最终产品定位

EDSP 最终不应该只叫：

```text
数据库告警接入平台
```

更准确应该是：

```text
企业数据安全告警运营平台
多源安全事件接入与智能分析平台
统一安全数据接入与告警降噪平台
```

核心产品能力：

```text
1. 多源接入
2. 原始数据留存
3. 标准事件归一化
4. 表/字段/日志语义识别
5. 扫描完整性核验
6. 试运行沙箱
7. 告警规则引擎
8. 风险评分模型
9. 本地小模型解释和核验
10. 告警降噪与优先级排序
11. 客户低操作交付体验
12. 模板与知识库持续沉淀
```

最终一句话：

> **EDSP 的核心不是一开始就知道所有厂家的标准答案，而是具备发现、核验、试运行、修正、学习和复用的能力。未知数据源第一次可控接入，第二次自动复用，第三次逐步标准化。**

---

# 结束语

今天讨论的最大共识是：

```text
入口可以很多：
数据库、Syslog、文件、API、Agent、消息队列、日志平台

但 EDSP 内部必须统一：
raw_data → standard_events → alerts → 通知/处置/报表/模型学习

不同厂家没有标准答案：
所以要靠数据库画像、语义识别、完整性核验、试运行沙箱和模板自学习机制来逐步建立答案。
```

这将成为 EDSP 后续产品化和交付能力的核心。
